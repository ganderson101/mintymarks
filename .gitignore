# Dependencies
node_modules/

# Build output
dist/
dev-dist/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
.venv/
venv/
env/

# Database (local dev only)
backend/mintymarks.db
*.db
*.sqlite

# Environment secrets
.env
.env.local
.env.*.local

# Editor
.DS_Store
.vscode/
.idea/
*.swp
*.swo
*.bak

# Logs
*.log
npm-debug.log*

# Test coverage
coverage/
.coverage
htmlcov/

# SQLite auxiliary files
*.db-journal
*.db-wal
*.db-shm
*.corrupt_backup

# Temp / working files (Claude scratchpad, restore backups, verify copies)
__coh_*
__restore_*
*.verify

# Board portal — invite tokens + PII, never committed
backend/board_members.json
# Seed config: child PII (emails) — never committed
backend/board_seed_config.json
# Generated login links — runtime output, not code
backend/board_login_links.txt
# Board inbox tickets are runtime state, not code
paperclip/state/board-inbox/*.md
